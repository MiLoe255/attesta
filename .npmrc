# Registry-Zuordnung fuer den eigenen Bereich. Der Zugangsschluessel steht nicht in
# dieser Datei, sondern kommt ueber ${GITHUB_TOKEN} zur Laufzeit herein; npm loest
# das in .npmrc selbst auf.
@miloe255:registry=https://npm.pkg.github.com
//npm.pkg.github.com/:_authToken=${GITHUB_TOKEN}
