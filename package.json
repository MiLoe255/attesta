{
  "name": "attesta",
  "version": "0.1.0",
  "private": true,
  "description": "Attesta Zyklus, Action, Konsole und Dokumentationsseite. Öffentlich, FSL-1.1-Apache-2.0.",
  "license": "SEE LICENSE IN LICENSE",
  "bin": {
    "attesta": "dist/konsole.js"
  },
  "scripts": {
    "generate": "tsx scripts/generate-ursachen.ts && tsx scripts/generate-konfigurator.ts",
    "build": "npm run generate && tsc -p . && node scripts/bundle.mjs",
    "test": "tsx --test test/*.test.ts",
    "release": "node scripts/release.mjs"
  },
  "dependencies": {
    "@actions/core": "^1.11.1",
    "@actions/github": "^6.0.0",
    "@miloe255/attesta-core": "^0.1.0",
    "js-yaml": "^4.1.0"
  },
  "devDependencies": {
    "@types/js-yaml": "^4.0.9",
    "@types/node": "^22.10.0",
    "esbuild": "^0.24.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
